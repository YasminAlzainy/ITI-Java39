$("save").click(
    function () {
        var name = $("name").text();
        var phone = $("phone").text();
        var email = $("email").text();
        var genderSelect = $("name").text();

        document.createElement

    }
)
